{
  "name": "specflow-demo-e2e-tests",
  "version": "1.0.0",
  "description": "Sprint 1 E2E tests for auth features",
  "scripts": {
    "test": "jest --verbose",
    "test:register": "jest --verbose auth-register",
    "test:login": "jest --verbose auth-login",
    "test:session": "jest --verbose auth-session"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/supertest": "^6.0.0",
    "jest": "^29.7.0",
    "supertest": "^6.3.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.3.0"
  }
}
